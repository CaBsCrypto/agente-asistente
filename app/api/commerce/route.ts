import{NextRequest,NextResponse}from"next/server";import{commerce,publicIntent}from"@/app/domain";export const runtime="nodejs";export async function GET(r:NextRequest){return NextResponse.json({mode:"demo",offers:commerce.searchOffers(r.nextUrl.searchParams.get("query")??"")})}export async function POST(r:NextRequest){try{const b=await r.json();switch(b.action){case"create_intent":{const x=commerce.createIntent(b);return NextResponse.json({...x,intent:publicIntent(x.intent)})}case"evaluate_policy":return NextResponse.json({intent:publicIntent(commerce.evaluatePolicy(b.intentId))});case"authorize":{const i=commerce.authorize(b.intentId,b.explicitUserConfirmation===true);return NextResponse.json({intent:publicIntent(i),authorizationToken:i.authorization?.token,warning:"DEMO ONLY"})}case"execute":return NextResponse.json({receipt:commerce.execute(b.intentId,b.authorizationToken)});case"get_receipt":return NextResponse.json({receipt:commerce.getReceipt(b.intentId)});default:return NextResponse.json({error:"unknown_action"},{status:400})}}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"unknown_error"},{status:400})}}

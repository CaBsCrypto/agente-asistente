# Engineering handoff — 2026-09-06

This is the restart point for the next Carmelita team. It records repository
state and evidence without credentials, user identifiers, or wallet addresses.

## Canonical state

- Repository: `CaBsCrypto/carmelita`
- Canonical branch: `main`
- Verified `main` commit: `c163e15`
- Production last directly confirmed during the wallet work: `c4918cc`
- Do not assume a branch is ready to merge merely because it exists remotely.

## Immediate next action

Review branch `fix/webmcp-type-contract`. It fixes incompatible duplicate
definitions of the browser WebMCP `modelContext.registerTool` contract and the
React effect used by the inspector.

- Commit: `0b18143`
- TypeScript: passed after the correction.
- Focused WebMCP lint: passed after the correction.
- Focused wallet/Solana tests: 15 passed.
- WebMCP runtime test: not executed locally because `tsx` intermittently fails
  to start `esbuild` with `spawn EPERM` on this Windows host.
- Local Next.js build: did not complete and was stopped after a bounded wait.
- Required gate: validate the branch in a clean CI/Linux or remote build before
  merging or deploying.

## Wallet and network status

| Network | Identifier | Asset | Rollout |
| --- | --- | --- | --- |
| Stellar Testnet | `stellar:testnet` | XLM | Active |
| Avalanche Fuji | `avalanche:fuji` | AVAX | Experimental |
| Solana Devnet | `solana:devnet` | SOL | Experimental |
| Base Sepolia | `base:sepolia` | ETH | Planned |
| BNB Smart Chain Testnet | `bnb:testnet` | tBNB | Planned |

Stellar reconnection is idempotent in `main`: an existing user-owned Stellar
wallet is reused, and MCP suppresses a stale pending entry when an active entry
for the same network exists. Admin intentionally retains all persisted records
as diagnostic evidence. Do not delete or rewrite real user wallet records as a
cleanup shortcut.

Solana Devnet onboarding, balance lookup, faucet endpoint, wallet UI, and
transaction preparation are present in `main`. Any funding, signing, or
submission acceptance test still requires explicit user authorization.

## Remote branches requiring review

These remote tips were not ancestors of `main` at handoff time:

- `feat/avalanche-x402-merchant-sdk`
- `feat/mcp-stage-a-hardening`
- `fix/apify-connection-claim`
- `fix/verify-avalanche-settlement-on-chain`

Treat them as review candidates, not an automatic merge queue. Determine
whether each is obsolete, superseded, or still contains unique safe changes.

## Recommended restart sequence

1. Fetch origin and confirm the remote `main` SHA before any integration.
2. Run TypeScript, lint, focused WebMCP tests, wallet tests, and the production
   build in a clean environment.
3. Review `fix/webmcp-type-contract`; merge without force only if all gates pass.
4. Audit the four remote branches above against current `main` one at a time.
5. Verify the deployed MCP/ChatGPT flow with an authenticated user session:
   Stellar Testnet, Avalanche Fuji, and Solana Devnet must show network and
   public address. Do not inspect browser storage or authorize funding/signing.
6. Update `docs/product-status.md` with dated deployment evidence after the
   production version is actually verified.

## Known environment limits

- Parallel `tsx --test tests/*.test.ts` fails on this Windows host with
  `spawn EPERM`; several tests pass when executed sequentially.
- Local Next.js/Vinext builds can stall or fail to provision child processes.
- Earlier Vercel previews failed before compilation with `Resource provisioning
  failed`; retry remotely and distinguish infrastructure failure from code
  failure before making a release decision.

No production deployment, wallet mutation, payment, funding, or signature was
performed as part of this handoff update.

# PR #28: consolidated baseline and isolated Preview

Evidence date: September 8, 2026 (UTC). This delivery updates the existing
`fix/webmcp-type-contract` proposal and preserves `HANDOFF-2026-09-06.md`.

## Validated baseline

- Node 24.14.0 / npm 11.6.1: clean installation succeeded; direct dependencies retained.
- Consolidated lint: no errors or warnings. Tests: 444 total, 442 passed, 0 failed, 2 skipped.
- The two explicit skips are live official MCP and live Dexalot Testnet checks, which require separate external acceptance.
- Next.js build and TypeScript completed. Local evidence is in [the dated evidence directory](audits/preview-pr28-2026-09-08/quality.txt).
- Shared WebMCP registration, cleanup, fresh session handling and HTTP errors retain the complete local implementation and tests. Direct Solana faucet registration is removed; malformed or unconfirmed funding requests fail closed.
- Bazaar, credentials and temporary working files are excluded from source/build/package inputs.

## Preview isolation

Project: `agente-asistente` / `prj_UQnTOdi1AWU6soTr04ACsqNo7YDu`.
QA database: `carmelita-preview-pr28`, Neon project `fancy-art-64243805`,
Vercel resource `store_XLsI5Pw9YPkHBbfA`. Created empty on the existing free plan;
no production data was copied and Neon Auth was not enabled.

The new integration is connected only to Preview under unused `CARMELITA_QA_*`
names. Runtime and migration variables are separately scoped to Preview **and**
`fix/webmcp-type-contract`: `DATABASE_URL`, `DATABASE_URL_UNPOOLED`,
`DATABASE_URL_DATABASE_URL`, `DATABASE_URL_DATABASE_URL_UNPOOLED`, plus the
explicit isolation host/origin/deployment controls. Every supported database
fallback points to QA. `UNBLCK_API_ENABLED=false` is branch-specific.

Database fingerprint: `bfd2efdf0f2fec5299c643ab9ef012c2648d8baa2d83297ff3f924b6bd123299`.
The runtime exposes only this hash to acceptance scripts. The canonical QA host
allowlist rejects any different resource, including production. Production's
hostname was not retrievable through its sensitive Vercel variable; identity
separation is established by the newly created resource and exact QA allowlist.

The independent guarded migration succeeded, followed by an idempotent repeat:
0 public tables before, 38 afterwards, 20 journal entries. Migration 0019 adds
the previously missing `agent_x402_events` table. Historical SQL remains intact.
DDL and journal updates now share a transaction with concurrency protection.
Historical LF/CRLF hashes are accepted only for equivalent line endings; new
entries use canonical LF, so Windows and Vercel recognize the same migrations.
Schema snapshots currently end at 0016; reconcile manual later migrations before
a future automatic schema generation. The historical journal's dialect label
does not affect the PostgreSQL runner.

Before push, Vercel's project-level build override was absent. Versioned
`vercel.json` now builds with `npm run build`; migrations run only through the
explicit independent Preview step. The production domain still resolves to
`dpl_tYAhcpzd9uxqAYPU6869jLzt7dbG` (Ready); the latest failed production target
is a different deployment and must not be confused with live traffic.

## Acceptance and remaining evidence

The runners require explicit URL/deployment/commit and verify remote environment,
commit and database fingerprint before authenticated writes. Bootstrap requires
an explicit flag and matching Origin. Financial execution is disabled in this
delivery. Gateway fixtures are synthetic and never count as Privy login proof.
Cleanup uses exact plan/token IDs, run-specific request IDs and scoped pseudonyms;
the report gives deleted counts and preserves shared or historical records.
See [acceptance testing](acceptance-testing.md) for the current interfaces.

Deployment URL/commit and remote results will be appended after the Preview is ready.
Two dedicated Privy identities, actual login/session recovery, wallet stability,
chat/memory persistence and browser acceptance remain pending until observed.
No payments, funding, reservations or Telegram calls are part of this delivery.

Graphify update was attempted after code changes and failed because its launcher
references an unavailable Python 3.12 executable. No updated graph is claimed.

Travala remains pending after the previously observed HTTP 401. Notion/OAuth,
Stellar payment acceptance and Bazaar consumption remain separate later gates.
